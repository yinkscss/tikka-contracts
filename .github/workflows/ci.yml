name: CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  build_and_test:
    name: Build, Test, and Lint
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4

    - name: Set up Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        toolchain: stable
        targets: wasm32-unknown-unknown
        components: clippy, rustfmt

    - name: Rust Cache
      uses: Swatinem/rust-cache@v2

    - name: Check Formatting
      run: cargo fmt --all -- --check

    - name: Build Contracts
      run: cargo build --target wasm32-unknown-unknown --release

    - name: Check WASM Binary Size (128KB limit)
      run: |
        MAX_SIZE=131072
        FAILED=0
        for wasm in target/wasm32-unknown-unknown/release/*.wasm; do
          [ -e "$wasm" ] || continue
          SIZE=$(stat -c%s "$wasm")
          echo "$wasm: ${SIZE} bytes"
          if [ "$SIZE" -gt "$MAX_SIZE" ]; then
            echo "ERROR: $wasm exceeds 128KB limit (${SIZE} > ${MAX_SIZE} bytes)"
            FAILED=1
          fi
        done
        exit $FAILED

    - name: Run Linter (Clippy)
      # Lib-only: --all-targets currently fails compiling soroban-env-host
      # testutils against ed25519-dalek 3 (upstream CryptoRng mismatch).
      run: cargo clippy -- -D warnings

    - name: Run Tests
      run: cargo test --workspace
      continue-on-error: true

  oracle_check:
    name: Oracle Service CI
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: oracle
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: oracle/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: TypeScript compile check
        run: npm run build
      - name: Run tests
        run: npm test
        continue-on-error: true
