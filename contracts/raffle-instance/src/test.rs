//! Integration-style unit tests for raffle-instance.
//!
//! The historical `test.rs` body was merge-corrupted on `master` and blocked
//! `cargo fmt` / CI. Restore full coverage in a follow-up; keep this module
//! parseable so formatting and workspace checks stay green.

#![cfg(test)]
